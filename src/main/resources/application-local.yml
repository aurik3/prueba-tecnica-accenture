spring:
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb://localhost:27017/franchise_db}
      auto-index-creation: true

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
